// math_main.cpp
// #include <iostream>
import math;
import math.mul;

int main(int argc, char *argv[]) {
  int a = 10, b = 20;
  // std::cout << "a + b = " << math::add(a, b) << "\n";
  // std::cout << "a - b = " << math::sub(a, b) << "\n";
  // std::cout << "a * b = " << math::mul(a, b) << "\n";
  int c = math::add(a, b);
  int d = math::sub(a, b);
  return 0;
}
